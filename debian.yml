---
- name: install packages
  apt: pkg={{item}} state=installed
  with_items:
    - git
    - vim
    - python-pip
    - python-dev
    - python-netaddr
    - libffi-dev
    - libssl-dev
    - gcc
    - git
    - bridge-utils
